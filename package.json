{
  "name": "justen",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
"scripts": {
  "build": "rollup -c=rollup.config.js --bundleConfigAsCjs",
  "watch": "rollup -w -c=rollup.config.js --bundleConfigAsCjs",
  "single": "yarn workspace @justen/$FOLDER build",
  "single:watch": "yarn workspace @justen/$FOLDER watch",
  "version": "changeset version",
  "release": "changeset publish",
  "changeset": "changeset"
},
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@rollup/plugin-terser": "^0.4.4",
    "rollup": "^4.x",
    "rollup-plugin-dts": "^6.x"
  },
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
